engine-strict=true

# Security settings
# Block packages released less than 24 hours ago
safe-chain-minimum-package-age=24h

# Block packages with security score below threshold (0-100)
safe-chain-minimum-security-score=70

# Always run security scan on install
safe-chain=true
